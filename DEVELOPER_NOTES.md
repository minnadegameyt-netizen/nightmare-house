# 開発者向けメモ (Developer Notes)

> [!CAUTION]
> **最優先ルール: カメラの手前側（南側/Z軸プラス方向）の壁は絶対にMeshとして描画しないでください。**
> 当たり判定（Collision）のみ記述し、`scene.add` は絶対に行わないでください。これを破るとプレイヤーの視界が完全に遮られます。

このプロジェクトの設計原則・ルールに関するメモです。他のAIや開発者が作業を引き継ぐ際は、必ずこのルールを参照してください。

## 1. 視点とカメラに関するルール
- カメラの方向は南から北へ見下ろす斜め俯瞰（アイソメトリック）カメラです。
- **重要: カメラの手前側（手前・南側）にあたる壁は、視界を遮るため絶対にメッシュ（Mesh）として描画（Sceneに追加）しないでください。**
  - 主人公の部屋の南側の壁（Z軸方向プラス側の壁）
  - 拡張エリア（姉の部屋など）の南側の壁
- ただし、表示を消すだけでなく、落下や壁抜けを防ぐため、`main.js` の `checkCollision` 関数内での**当たり判定（AABB）の記述は必ず残すようにしてください**。（目に見えない壁として機能させるため）

## 2. 当たり判定（Collision）のルール
- 廊下や複雑な形状のマップを作る際、単純なX,Z座標の範囲指定（inBounds処理）だと、ドアの隙間などをすり抜けるバグが発生しやすいです。
- 必ず全オブジェクトのAABB（Axis-Aligned Bounding Box: `[minX, maxX, minZ, maxZ]`）を配列化し、プレイヤーの半径（Circle）と衝突判定を行う、現状の `checkCollision` のロジックをベースに拡張してください。

## 3. テキスト・音のルール
- 現状、音声アセットを使わずにWebAPIやCSSでホラー演出を作っています。「ガチャ」等の環境音はUIテキストや画面のグリッチ（Flash）で表現し、Asset無しのプログラマビリティを維持してください。
